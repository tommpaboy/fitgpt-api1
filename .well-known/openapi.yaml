from datetime import date

openapi_yaml = f"""
openapi: 3.0.0
info:
  title: FitGPT API
  version: 1.0.0
servers:
  - url: https://fitgpt-2364.onrender.com
paths:
  /data:
    get:
      summary: Hämta all tillgänglig Fitbit-data
      description: Hämtar Fitbit-data för aktuell dag eller valfritt datum (ex. steg, sömn, träning, puls, kalorier).
      parameters:
        - in: query
          name: date
          schema:
            type: string
            example: "{date.today().isoformat()}"
          description: Datum i formatet YYYY-MM-DD. Om inget anges används dagens datum.
      responses:
        '200':
          description: Returnerar Fitbit-data för angivet datum.
          content:
            application/json:
              schema:
                type: object
"""

# Spara till fil
path = "/mnt/data/openapi.yaml"
with open(path, "w") as f:
    f.write(openapi_yaml)

path
